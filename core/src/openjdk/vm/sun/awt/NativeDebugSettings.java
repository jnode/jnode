package sun.awt;

/**
 * @see sun.awt.DebugSettings
 */
class NativeDebugSettings {
    private static synchronized void setCTracingOn(DebugSettings instance, boolean enabled) {
        //todo implement it
    }
    private static synchronized void setCTracingOn(DebugSettings instance, boolean enabled, String file) {
        //todo implement it
    }
    private static synchronized void setCTracingOn(DebugSettings instance, boolean enabled, String file, int line) {
        //todo implement it
    }
}
